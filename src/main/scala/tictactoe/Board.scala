package tictactoe

class Board(val actor: BoardActor) {
//  def join(newPlayer: Player): Unit = {
//    players += newPlayer
//    broadcast(PlayerActor.PlayerJoined(newPlayer))
//  }
//
//  def leave(player: Player): Unit = {
//    players -= player
//    broadcast(PlayerActor.PlayerLeft(player))
//  }
//
//  def broadcast(message: PlayerActor.OutgoingMessage): Unit = {
//    players.foreach(_.actor ! message)
//  }
//
//  var players: Set[Player] = Set.empty[Player]
//  var currentTurn = 0
//  var ready = false

}
